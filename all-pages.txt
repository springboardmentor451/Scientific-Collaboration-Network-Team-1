========== institutions.jsx ==========
import React, { useState } from 'react';
import Sidebar from '../components/common/Sidebar';
import { Search, Plus, Building2, Globe, BookOpen, Handshake, ExternalLink } from 'lucide-react';

const institutionsData = [
  {
    id: 1,
    name: 'Tech Institute of Science',
    type: 'Primary Campus',
    country: 'United States',
    city: 'Boston, MA',
    jointPublications: 1240,
    activeProjects: 18,
    tier: 'Tier 1 Partner',
    website: 'https://example.edu',
  },
  {
    id: 2,
    name: 'Genome Research Center',
    type: 'Affiliated Institute',
    country: 'United Kingdom',
    city: 'Cambridge',
    jointPublications: 680,
    activeProjects: 9,
    tier: 'Tier 1 Partner',
    website: 'https://example.ac.uk',
  },
  {
    id: 3,
    name: 'Quantum Labs Intl.',
    type: 'Industry Research Lab',
    country: 'Switzerland',
    city: 'Zurich',
    jointPublications: 410,
    activeProjects: 6,
    tier: 'Industry Partner',
    website: 'https://example.ch',
  },
  {
    id: 4,
    name: 'National Center for Supercomputing',
    type: 'Government Facility',
    country: 'Germany',
    city: 'Munich',
    jointPublications: 290,
    activeProjects: 4,
    tier: 'Strategic Partner',
    website: 'https://example.de',
  },
];

export default function Institutions() {
  const [searchTerm, setSearchTerm] = useState('');
  const [typeFilter, setTypeFilter] = useState('All');

  const filteredInstitutions = institutionsData.filter((inst) => {
    const matchesSearch =
      inst.name.toLowerCase().includes(searchTerm.toLowerCase()) ||
      inst.country.toLowerCase().includes(searchTerm.toLowerCase()) ||
      inst.city.toLowerCase().includes(searchTerm.toLowerCase());

    if (typeFilter === 'All') return matchesSearch;
    return matchesSearch && inst.type === typeFilter;
  });

  return (
    <div className="flex min-h-screen bg-slate-50">
      <Sidebar />

      <main className="flex-1 p-8 overflow-y-auto">
        {/* Header */}
        <div className="flex justify-between items-center mb-6">
          <div>
            <h1 className="text-2xl font-bold text-slate-900">Partner Institutions</h1>
            <p className="text-xs text-slate-500">
              Manage global academic, government, and industry research network partnerships.
            </p>
          </div>
          <button className="bg-indigo-600 hover:bg-indigo-700 text-white text-xs font-semibold px-4 py-2 rounded-lg flex items-center space-x-2 transition">
            <Plus className="w-4 h-4" />
            <span>Add Partner Institution</span>
          </button>
        </div>

        {/* Filter Controls */}
        <div className="flex flex-col md:flex-row gap-4 justify-between items-center mb-6 bg-white p-4 rounded-xl border border-slate-200">
          <div className="relative w-full md:w-96">
            <Search className="w-4 h-4 absolute left-3 top-3 text-slate-400" />
            <input
              type="text"
              placeholder="Search institution, city, or country..."
              value={searchTerm}
              onChange={(e) => setSearchTerm(e.target.value)}
              className="w-full pl-9 pr-4 py-2 bg-slate-50 border border-slate-200 rounded-lg text-xs focus:ring-2 focus:ring-indigo-500 outline-none"
            />
          </div>

          <div className="flex items-center space-x-2">
            {['All', 'Primary Campus', 'Affiliated Institute', 'Industry Research Lab'].map((type) => (
              <button
                key={type}
                onClick={() => setTypeFilter(type)}
                className={`px-3 py-1.5 text-xs font-medium rounded-lg transition ${
                  typeFilter === type
                    ? 'bg-indigo-600 text-white'
                    : 'bg-slate-100 text-slate-600 hover:bg-slate-200'
                }`}
              >
                {type}
              </button>
            ))}
          </div>
        </div>

        {/* Institutions Grid */}
        <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
          {filteredInstitutions.map((inst) => (
            <div key={inst.id} className="bg-white rounded-xl border border-slate-200 p-6 shadow-sm hover:shadow-md transition">
              <div className="flex justify-between items-start mb-3">
                <div className="flex items-center space-x-3">
                  <div className="w-10 h-10 rounded-lg bg-slate-100 border border-slate-200 flex items-center justify-center text-slate-700 font-bold">
                    <Building2 className="w-5 h-5 text-indigo-600" />
                  </div>
                  <div>
                    <h3 className="font-bold text-slate-900 text-sm hover:text-indigo-600 cursor-pointer">
                      {inst.name}
                    </h3>
                    <p className="text-[11px] text-slate-400 font-medium">
                      {inst.city}, {inst.country}
                    </p>
                  </div>
                </div>
                <span className="px-2.5 py-0.5 bg-indigo-50 text-indigo-600 text-[10px] font-semibold rounded-full">
                  {inst.tier}
                </span>
              </div>

              {/* Metrics Grid */}
              <div className="grid grid-cols-2 gap-3 bg-slate-50 p-3 rounded-lg text-xs my-4">
                <div>
                  <span className="text-[10px] font-semibold text-slate-400 block flex items-center space-x-1">
                    <BookOpen className="w-3 h-3 text-indigo-500" />
                    <span>JOINT PUBLICATIONS</span>
                  </span>
                  <span className="font-bold text-slate-800">{inst.jointPublications} Papers</span>
                </div>

                <div>
                  <span className="text-[10px] font-semibold text-slate-400 block flex items-center space-x-1">
                    <Handshake className="w-3 h-3 text-indigo-500" />
                    <span>ACTIVE PROJECTS</span>
                  </span>
                  <span className="font-bold text-slate-800">{inst.activeProjects} Grants</span>
                </div>
              </div>

              {/* Footer */}
              <div className="flex items-center justify-between text-xs pt-3 border-t">
                <span className="text-slate-500 font-medium">
                  Type: <strong className="text-slate-700">{inst.type}</strong>
                </span>
                <a
                  href={inst.website}
                  target="_blank"
                  rel="noreferrer"
                  className="text-indigo-600 hover:text-indigo-700 font-semibold flex items-center space-x-1"
                >
                  <span>Portal</span>
                  <ExternalLink className="w-3 h-3" />
                </a>
              </div>
            </div>
          ))}
        </div>
      </main>
    </div>
  );
}

========== citations.jsx ==========
import React, { useState } from 'react';
import Sidebar from '../components/common/Sidebar';
import { Search, Quote, TrendingUp, Award, ArrowUpRight, BookOpen, ExternalLink, Filter } from 'lucide-react';

const topCitedPapers = [
  {
    id: 1,
    title: 'Cross-Institutional Proteomic Data Mapping using Transformer Architectures',
    author: 'Prof. Elena Rostova et al.',
    journal: 'Nature Biotechnology',
    year: 2024,
    citations: 310,
    growthRate: '+34% YoY',
    field: 'Bioinformatics',
  },
  {
    id: 2,
    title: 'Scalable Graph Neural Networks for Large-Scale Scientific Network Analysis',
    author: 'Dr. Aris Thorne et al.',
    journal: 'IEEE TPAMI',
    year: 2025,
    citations: 142,
    growthRate: '+88% YoY',
    field: 'Computer Science',
  },
  {
    id: 3,
    title: 'Fault-Tolerant Quantum Circuit Mapping on Scalable Processing Units',
    author: 'Dr. Marcus Vance et al.',
    journal: 'ICQC Proceedings',
    year: 2024,
    citations: 89,
    growthRate: '+22% YoY',
    field: 'Quantum Computing',
  },
  {
    id: 4,
    title: 'Ethical Guidelines and Safety Metrics in Scientific Machine Learning',
    author: 'Dr. Sarah Chen',
    journal: 'AI & Society Series',
    year: 2023,
    citations: 45,
    growthRate: '+12% YoY',
    field: 'AI Safety',
  },
];

const impactMetrics = [
  { label: 'TOTAL CITATIONS', value: '48,920', sub: '+18.9% this year', color: 'text-emerald-600' },
  { label: 'INSTITUTIONAL H-INDEX', value: '64', sub: 'Top 5% globally', color: 'text-indigo-600' },
  { label: 'AVG CITATIONS / PAPER', value: '5.21', sub: '+0.8 vs baseline', color: 'text-indigo-600' },
  { label: 'TOP 1% MOST CITED PAPERS', value: '18', sub: 'High impact output', color: 'text-amber-600' },
];

export default function Citations() {
  const [searchTerm, setSearchTerm] = useState('');
  const [selectedField, setSelectedField] = useState('All');

  const filteredPapers = topCitedPapers.filter((paper) => {
    const matchesSearch =
      paper.title.toLowerCase().includes(searchTerm.toLowerCase()) ||
      paper.author.toLowerCase().includes(searchTerm.toLowerCase()) ||
      paper.journal.toLowerCase().includes(searchTerm.toLowerCase());

    if (selectedField === 'All') return matchesSearch;
    return matchesSearch && paper.field === selectedField;
  });

  return (
    <div className="flex min-h-screen bg-slate-50">
      <Sidebar />

      <main className="flex-1 p-8 overflow-y-auto">
        {/* Header */}
        <div className="flex justify-between items-center mb-6">
          <div>
            <h1 className="text-2xl font-bold text-slate-900">Citation Metrics & Impact</h1>
            <p className="text-xs text-slate-500">
              Track citation velocity, paper impact factors, and institutional scholarly influence.
            </p>
          </div>
        </div>

        {/* Impact Cards Bar */}
        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4 mb-8">
          {impactMetrics.map((item, idx) => (
            <div key={idx} className="bg-white p-5 rounded-xl border border-slate-200 shadow-sm">
              <span className="text-[10px] font-semibold tracking-wider text-slate-400 block mb-1">
                {item.label}
              </span>
              <div className="text-2xl font-bold text-slate-900 mb-1">{item.value}</div>
              <span className={`text-[11px] font-semibold ${item.color}`}>{item.sub}</span>
            </div>
          ))}
        </div>

        {/* Filter Toolbar */}
        <div className="flex flex-col md:flex-row gap-4 justify-between items-center mb-6 bg-white p-4 rounded-xl border border-slate-200">
          <div className="relative w-full md:w-96">
            <Search className="w-4 h-4 absolute left-3 top-3 text-slate-400" />
            <input
              type="text"
              placeholder="Search paper title, author, or journal..."
              value={searchTerm}
              onChange={(e) => setSearchTerm(e.target.value)}
              className="w-full pl-9 pr-4 py-2 bg-slate-50 border border-slate-200 rounded-lg text-xs focus:ring-2 focus:ring-indigo-500 outline-none"
            />
          </div>

          <div className="flex items-center space-x-2">
            {['All', 'Bioinformatics', 'Computer Science', 'Quantum Computing'].map((field) => (
              <button
                key={field}
                onClick={() => setSelectedField(field)}
                className={`px-3 py-1.5 text-xs font-medium rounded-lg transition ${
                  selectedField === field
                    ? 'bg-indigo-600 text-white'
                    : 'bg-slate-100 text-slate-600 hover:bg-slate-200'
                }`}
              >
                {field}
              </button>
            ))}
          </div>
        </div>

        {/* Top Cited Papers Table / List */}
        <div className="bg-white rounded-xl border border-slate-200 shadow-sm overflow-hidden">
          <div className="p-5 border-b border-slate-100 flex justify-between items-center">
            <h3 className="text-sm font-bold text-slate-900 flex items-center space-x-2">
              <Quote className="w-4 h-4 text-indigo-600" />
              <span>Highest Cited Publications</span>
            </h3>
            <span className="text-xs text-slate-400">Ranked by citation velocity</span>
          </div>

          <div className="divide-y divide-slate-100">
            {filteredPapers.map((paper) => (
              <div key={paper.id} className="p-5 hover:bg-slate-50/80 transition flex items-center justify-between gap-4">
                <div className="space-y-1 flex-1">
                  <div className="flex items-center space-x-2">
                    <span className="px-2 py-0.5 bg-slate-100 text-slate-600 text-[10px] font-semibold rounded">
                      {paper.field}
                    </span>
                    <span className="text-xs text-slate-400">• {paper.year}</span>
                  </div>
                  <h4 className="text-sm font-bold text-slate-900 hover:text-indigo-600 cursor-pointer">
                    {paper.title}
                  </h4>
                  <p className="text-xs text-slate-500">
                    {paper.author} — <span className="italic">{paper.journal}</span>
                  </p>
                </div>

                <div className="flex items-center space-x-6 text-right">
                  <div>
                    <span className="text-[10px] font-semibold text-slate-400 block">GROWTH</span>
                    <span className="text-xs font-bold text-emerald-600 flex items-center space-x-0.5 justify-end">
                      <TrendingUp className="w-3 h-3 inline" />
                      <span>{paper.growthRate}</span>
                    </span>
                  </div>

                  <div className="bg-indigo-50 border border-indigo-100 px-4 py-2 rounded-xl text-center min-w-[90px]">
                    <span className="text-[10px] font-semibold text-indigo-400 block">CITATIONS</span>
                    <span className="text-lg font-bold text-indigo-600">{paper.citations}</span>
                  </div>
                </div>
              </div>
            ))}
          </div>
        </div>
      </main>
    </div>
  );
}


========== analytics.jsx ==========
import React, { useState } from 'react';
import Sidebar from '../components/common/Sidebar';
import { BarChart3, TrendingUp, PieChart, Download, Calendar, Filter, Award, BookOpen, Network, Users } from 'lucide-react';

const analyticsSummary = [
  { label: 'ANNUAL CITATION GROWTH', value: '+18.9%', sub: '48,920 Total Citations', icon: TrendingUp, color: 'text-emerald-600' },
  { label: 'H-INDEX AVERAGE', value: '31.4', sub: 'Across 1,482 Researchers', icon: Award, color: 'text-indigo-600' },
  { label: 'COLLABORATION INDEX', value: '0.78', sub: '+0.12 vs Previous Year', icon: Network, color: 'text-indigo-600' },
  { label: 'OPEN ACCESS RATIO', value: '64.2%', sub: '6,019 OA Publications', icon: BookOpen, color: 'text-emerald-600' },
];

const departmentOutput = [
  { department: 'Computer Science & AI', pubs: 3420, percentage: 36.4, color: 'bg-indigo-600' },
  { department: 'Bioinformatics & Genetics', pubs: 2310, percentage: 24.6, color: 'bg-emerald-500' },
  { department: 'Quantum Physics', pubs: 1840, percentage: 19.6, color: 'bg-purple-600' },
  { department: 'Data Science & Systems', pubs: 1210, percentage: 12.9, color: 'bg-amber-500' },
  { department: 'Cognitive Science', pubs: 596, percentage: 6.5, color: 'bg-rose-500' },
];

export default function Analytics() {
  const [timeRange, setTimeRange] = useState('2025 - 2026');

  return (
    <div className="flex min-h-screen bg-slate-50">
      <Sidebar />

      <main className="flex-1 p-8 overflow-y-auto">
        {/* Header */}
        <div className="flex justify-between items-center mb-6">
          <div>
            <h1 className="text-2xl font-bold text-slate-900">Research Analytics</h1>
            <p className="text-xs text-slate-500">
              Institutional performance indicators, department distributions, and impact metrics.
            </p>
          </div>
          <div className="flex items-center space-x-3">
            <div className="flex items-center space-x-2 bg-white px-3 py-1.5 border border-slate-200 rounded-lg text-xs font-semibold text-slate-600">
              <Calendar className="w-3.5 h-3.5 text-slate-400" />
              <select 
                value={timeRange} 
                onChange={(e) => setTimeRange(e.target.value)}
                className="bg-transparent outline-none cursor-pointer"
              >
                <option>2025 - 2026</option>
                <option>2023 - 2024</option>
                <option>2021 - 2022</option>
              </select>
            </div>
            <button className="bg-indigo-600 hover:bg-indigo-700 text-white text-xs font-semibold px-4 py-2 rounded-lg flex items-center space-x-2 transition">
              <Download className="w-4 h-4" />
              <span>Export Report</span>
            </button>
          </div>
        </div>

        {/* Overview Metric Cards */}
        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4 mb-8">
          {analyticsSummary.map((item, idx) => {
            const Icon = item.icon;
            return (
              <div key={idx} className="bg-white p-5 rounded-xl border border-slate-200 shadow-sm">
                <div className="flex justify-between items-center mb-2">
                  <span className="text-[10px] font-semibold tracking-wider text-slate-400">
                    {item.label}
                  </span>
                  <Icon className={`w-4 h-4 ${item.color}`} />
                </div>
                <div className="text-2xl font-bold text-slate-900">{item.value}</div>
                <span className="text-[11px] text-slate-500 font-medium">{item.sub}</span>
              </div>
            );
          })}
        </div>

        {/* Analytics Breakdown Grid */}
        <div className="grid grid-cols-1 lg:grid-cols-3 gap-6">
          {/* Output Chart Canvas Representation */}
          <div className="lg:col-span-2 bg-white rounded-xl border border-slate-200 p-6 shadow-sm">
            <div className="flex justify-between items-center mb-6">
              <div>
                <h3 className="text-sm font-bold text-slate-900">Publication & Citation Trajectory</h3>
                <p className="text-[11px] text-slate-400">Year-over-year research output comparison</p>
              </div>
              <div className="flex items-center space-x-4 text-xs font-medium text-slate-500">
                <span className="flex items-center space-x-1">
                  <span className="w-2.5 h-2.5 rounded bg-indigo-600 inline-block"></span>
                  <span>Publications</span>
                </span>
                <span className="flex items-center space-x-1">
                  <span className="w-2.5 h-2.5 rounded bg-emerald-500 inline-block"></span>
                  <span>Citations (x10)</span>
                </span>
              </div>
            </div>

            {/* Visual Bar Graph Simulation */}
            <div className="h-64 flex items-end justify-between gap-4 pt-8 px-4 border-b border-slate-100">
              {[
                { year: '2021', pub: 40, cit: 55 },
                { year: '2022', pub: 55, cit: 68 },
                { year: '2023', pub: 70, cit: 82 },
                { year: '2024', pub: 85, cit: 95 },
                { year: '2025', pub: 92, cit: 110 },
                { year: '2026', pub: 100, cit: 130 },
              ].map((bar, i) => (
                <div key={i} className="flex-1 flex flex-col items-center gap-2 h-full justify-end">
                  <div className="w-full flex justify-center items-end gap-1.5 h-full">
                    <div 
                      className="w-1/2 bg-indigo-600 rounded-t transition-all duration-500 hover:bg-indigo-700" 
                      style={{ height: `${bar.pub}%` }}
                    ></div>
                    <div 
                      className="w-1/2 bg-emerald-500 rounded-t transition-all duration-500 hover:bg-emerald-600" 
                      style={{ height: `${bar.cit * 0.7}%` }}
                    ></div>
                  </div>
                  <span className="text-[11px] font-semibold text-slate-400">{bar.year}</span>
                </div>
              ))}
            </div>
          </div>

          {/* Department Share Breakdown */}
          <div className="bg-white rounded-xl border border-slate-200 p-6 shadow-sm">
            <h3 className="text-sm font-bold text-slate-900 mb-1">Output by Department</h3>
            <p className="text-[11px] text-slate-400 mb-6">Distribution of total publications</p>

            <div className="space-y-4">
              {departmentOutput.map((dept, idx) => (
                <div key={idx}>
                  <div className="flex justify-between items-center text-xs mb-1 font-medium">
                    <span className="text-slate-700">{dept.department}</span>
                    <span className="font-bold text-slate-900">{dept.pubs} ({dept.percentage}%)</span>
                  </div>
                  <div className="w-full bg-slate-100 rounded-full h-2">
                    <div
                      className={`${dept.color} h-2 rounded-full`}
                      style={{ width: `${dept.percentage}%` }}
                    ></div>
                  </div>
                </div>
              ))}
            </div>
          </div>
        </div>
      </main>
    </div>
  );
}


========== reports.jsx ==========
import React, { useState } from 'react';
import Sidebar from '../components/common/Sidebar';
import { FileText, Download, Calendar, Filter, FileSpreadsheet, CheckCircle, Clock } from 'lucide-react';

const availableReports = [
  {
    id: 1,
    title: 'Annual Institutional Research Output Report',
    category: 'Publication & Citation Summary',
    format: 'PDF',
    generatedDate: 'Aug 15, 2026',
    size: '4.2 MB',
  },
  {
    id: 2,
    title: 'Grant Allocation & Financial Audit Overview',
    category: 'Project Finance',
    format: 'CSV',
    generatedDate: 'Aug 10, 2026',
    size: '1.8 MB',
  },
  {
    id: 3,
    title: 'Cross-Departmental Collaboration Metrics',
    category: 'Network Analytics',
    format: 'PDF',
    generatedDate: 'Jul 28, 2026',
    size: '3.1 MB',
  },
  {
    id: 4,
    title: 'ORCID & Scopus Synchronization Log',
    category: 'Integrations',
    format: 'CSV',
    generatedDate: 'Jul 15, 2026',
    size: '850 KB',
  },
];

export default function Reports() {
  const [reportType, setReportType] = useState('All');

  return (
    <div className="flex min-h-screen bg-slate-50">
      <Sidebar />

      <main className="flex-1 p-8 overflow-y-auto">
        {/* Header */}
        <div className="flex justify-between items-center mb-6">
          <div>
            <h1 className="text-2xl font-bold text-slate-900">Reports & Export Center</h1>
            <p className="text-xs text-slate-500">
              Generate, schedule, and download institutional compliance, funding, and impact statements.
            </p>
          </div>
          <button className="bg-indigo-600 hover:bg-indigo-700 text-white text-xs font-semibold px-4 py-2 rounded-lg flex items-center space-x-2 transition">
            <FileText className="w-4 h-4" />
            <span>Generate New Report</span>
          </button>
        </div>

        {/* Quick Generator Panel */}
        <div className="bg-white rounded-xl border border-slate-200 p-6 mb-8 shadow-sm">
          <h3 className="text-sm font-bold text-slate-900 mb-1">Custom Export Builder</h3>
          <p className="text-xs text-slate-400 mb-4">Select dataset parameters to export custom CSV or PDF reports.</p>

          <div className="grid grid-cols-1 md:grid-cols-3 gap-4 mb-4">
            <div>
              <label className="block text-xs font-semibold text-slate-700 mb-1">Report Domain</label>
              <select className="w-full p-2 bg-slate-50 border border-slate-200 rounded-lg text-xs font-medium text-slate-700 outline-none">
                <option>Publications & Citations</option>
                <option>Grant & Project Funding</option>
                <option>Researcher Profiles</option>
                <option>Institutional Collaborations</option>
              </select>
            </div>

            <div>
              <label className="block text-xs font-semibold text-slate-700 mb-1">Date Range</label>
              <select className="w-full p-2 bg-slate-50 border border-slate-200 rounded-lg text-xs font-medium text-slate-700 outline-none">
                <option>Year to Date (2026)</option>
                <option>Previous Academic Year (2025)</option>
                <option>Last 3 Years</option>
              </select>
            </div>

            <div>
              <label className="block text-xs font-semibold text-slate-700 mb-1">Format</label>
              <select className="w-full p-2 bg-slate-50 border border-slate-200 rounded-lg text-xs font-medium text-slate-700 outline-none">
                <option>PDF Document (.pdf)</option>
                <option>Excel / CSV Table (.csv)</option>
              </select>
            </div>
          </div>

          <div className="flex justify-end">
            <button className="bg-slate-900 hover:bg-slate-800 text-white text-xs font-semibold px-4 py-2 rounded-lg transition flex items-center space-x-2">
              <Download className="w-3.5 h-3.5" />
              <span>Compile & Download</span>
            </button>
          </div>
        </div>

        {/* Generated Reports List */}
        <div className="bg-white rounded-xl border border-slate-200 shadow-sm overflow-hidden">
          <div className="p-5 border-b border-slate-100 flex justify-between items-center">
            <h3 className="text-sm font-bold text-slate-900">Archive & Scheduled Reports</h3>
            <span className="text-xs text-slate-400">Showing recently compiled exports</span>
          </div>

          <div className="divide-y divide-slate-100">
            {availableReports.map((report) => (
              <div key={report.id} className="p-5 hover:bg-slate-50 transition flex items-center justify-between">
                <div className="flex items-center space-x-4">
                  <div className="p-2.5 rounded-lg bg-indigo-50 border border-indigo-100 text-indigo-600">
                    {report.format === 'PDF' ? (
                      <FileText className="w-5 h-5" />
                    ) : (
                      <FileSpreadsheet className="w-5 h-5" />
                    )}
                  </div>
                  <div>
                    <h4 className="text-sm font-bold text-slate-900">{report.title}</h4>
                    <p className="text-xs text-slate-500 font-medium">
                      {report.category} • <span className="text-slate-400">{report.size}</span>
                    </p>
                  </div>
                </div>

                <div className="flex items-center space-x-4 text-xs font-medium">
                  <span className="text-slate-400 flex items-center space-x-1">
                    <Calendar className="w-3.5 h-3.5" />
                    <span>{report.generatedDate}</span>
                  </span>
                  <button className="text-indigo-600 hover:text-indigo-700 font-semibold flex items-center space-x-1 border border-indigo-100 bg-indigo-50 px-3 py-1.5 rounded-lg">
                    <Download className="w-3.5 h-3.5" />
                    <span>Download</span>
                  </button>
                </div>
              </div>
            ))}
          </div>
        </div>
      </main>
    </div>
  );
}


========== settings.jsx ==========
import React, { useState } from 'react';
import Sidebar from '../components/common/Sidebar';
import { useData } from '../context/DataContext';
import { Save, CheckCircle2 } from 'lucide-react';

export default function Settings() {
  const { currentUser, updateProfile } = useData();
  const [formData, setFormData] = useState(currentUser);
  const [saved, setSaved] = useState(false);

  const handleSubmit = (e) => {
    e.preventDefault();
    updateProfile(formData);
    setSaved(true);
    setTimeout(() => setSaved(false), 3000);
  };

  return (
    <div className="flex min-h-screen bg-slate-50">
      <Sidebar />
      <main className="flex-1 p-8 overflow-y-auto">
        <h1 className="text-2xl font-bold text-slate-900 mb-6">Profile Settings</h1>

        {saved && (
          <div className="mb-4 p-3 bg-emerald-50 text-emerald-700 text-xs font-semibold rounded-lg flex items-center space-x-2">
            <CheckCircle2 className="w-4 h-4" />
            <span>Profile updated successfully across system!</span>
          </div>
        )}

        <form onSubmit={handleSubmit} className="bg-white p-6 rounded-xl border border-slate-200 max-w-xl space-y-4">
          <div>
            <label className="block text-xs font-semibold text-slate-700 mb-1">Full Name</label>
            <input
              type="text"
              value={formData.name}
              onChange={(e) => setFormData({ ...formData, name: e.target.value })}
              className="w-full p-2 bg-slate-50 border border-slate-200 rounded text-xs"
            />
          </div>

          <div>
            <label className="block text-xs font-semibold text-slate-700 mb-1">Department</label>
            <input
              type="text"
              value={formData.department}
              onChange={(e) => setFormData({ ...formData, department: e.target.value })}
              className="w-full p-2 bg-slate-50 border border-slate-200 rounded text-xs"
            />
          </div>

          <button
            type="submit"
            className="bg-indigo-600 text-white text-xs font-semibold px-4 py-2 rounded-lg flex items-center space-x-2"
          >
            <Save className="w-4 h-4" />
            <span>Save Profile Changes</span>
          </button>
        </form>
      </main>
    </div>
  );
}

========== audit.jsx ==========

