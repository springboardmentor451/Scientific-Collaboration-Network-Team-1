info:
  name: get-by-id
  type: http
  seq: 3

http:
  method: GET
  url: "{{researcherUrl}}/{{researcherId}}"
  headers: []
  body:
    type: none
  auth:
    type: none

runtime:
  scripts:
    - type: tests
      code: |
        test("status is 200", function() {
          expect(res.status).to.equal(200);
        });
        test("matches researcherId", function() {
          expect(res.body.researcher_id).to.equal(parseInt(bru.getVar("researcherId")));
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
  