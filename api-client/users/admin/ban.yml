info:
  name: ban
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{userUrl}}/{{userId}}/ban"
  auth:
    type: bearer
    token: "{{accessToken}}"

runtime:
  scripts:
    - type: tests
      code: |
        test("status is 200", function() {
          expect(res.status).to.equal(200);
        });
        test("status is banned", function() {
          expect(res.body.status).to.equal("banned");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
