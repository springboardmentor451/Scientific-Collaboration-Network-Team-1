info:
  name: update
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{conferenceUrl}}/{{conferenceId}}"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |
      {
        "name": null,
        "description": null,
        "location": null,
        "start_date": null,
        "end_date": null,
        "website": null
      }
  auth:
    type: bearer
    token: "{{accessToken}}"

runtime:
  scripts:
    - type: tests
      code: |
        test("status is 200", function() {
          expect(res.status).to.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
