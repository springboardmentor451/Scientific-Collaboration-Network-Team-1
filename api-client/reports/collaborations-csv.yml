info:
  name: collaborations-csv
  type: http
  seq: 3

http:
  method: POST
  url: "{{reportUrl}}/collaborations/csv"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |
      {
        "researcher_id": "",
        "from_date": "",
        "to_date": ""
      }
  auth:
    type: bearer
    token: "{{accessToken}}"

runtime:
  scripts:
    - type: tests
      code: |
        test("status is 200", function() {
          expect(res.status).to.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
