info:
  name: get-by-id
  type: http
  seq: 3

http:
  method: GET
  url: "{{institutionUrl}}/{{institutionId}}"
  headers: []
  body:
    type: none
  auth:
    type: none

runtime:
  scripts:
    - type: tests
      code: |
        test("status is 200", function() {
          expect(res.status).to.equal(200);
        });
        test("matches institutionId", function() {
          expect(res.body.institution_id).to.equal(parseInt(bru.getVar("institutionId")));
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
